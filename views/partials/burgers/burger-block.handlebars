<li>
	{{burger_name}}

	<button class="change-devorred" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Devour!!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
